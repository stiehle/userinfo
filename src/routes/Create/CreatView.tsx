import UserForm from "../../components/UserForm/UserForm";
import "./CreateView.scss";

function CreateView() {
  return (
    <>
      <UserForm user={null} />
    </>
  );
}

export default CreateView;
